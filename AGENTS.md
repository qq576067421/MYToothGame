# AI 助手规则

## 编码规则
- 所有 `.cs` 文件必须使用 **UTF-8 编码（带 BOM）** 保存，无论是新建还是修改。
- 编辑已有文件时保留原文件的编码格式（若原文件为 UTF-8 则继续使用 UTF-8）。

## 代码规范
- 不添加不必要的注释，除非需要解释复杂逻辑。
- 保持与项目现有代码一致的风格和命名惯例。
- 字段命名统一使用 `m_XxxYyy` 风格。
- 新增文件时遵循项目目录结构，不随意放置。

## 执行规则
- 修改前先阅读相关文件的上下文，理解代码逻辑。
- 修改后如有可用的 lint/编译检查命令，应运行确认无报错。
